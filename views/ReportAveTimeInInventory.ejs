<head>
    <meta charset="utf-8">
    <title>Ramblin Wrecks</title>
    <link rel="stylesheet" href="../css/main.css" type="text/css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.0/jquery.min.js"></script>
</head>

<body>
    <div class="search-results">
        <h1>Average Time In Inventory Report</h1>
        <% if(vehicles.length === 0) { %>
            <h2>Sorry, it looks like we don’t have that in stock!</h2>
        <% } else { %>
        <table>
            <tr>
                <th>VehicleType</th>
                <th>AVG_Time_In_Inv (days)</th>
            </tr>
                <% vehicles.forEach((v) => { %>
                    <tr>
                        <td><%= v.VehicleType %></td>
                        <td><%= v.AVG_Time_In_Inv %></td>
                    </tr>
                <%  }); %>
        </table>
    <% } %> 
    </div>
</body>
